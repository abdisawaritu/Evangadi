// let add = (x, y ) =>  x+y;

// export default add;

let add = (x, y) => x + y;
let sub = (x, y) => x - y;

export { add, sub };
export default add; // only one function to export  we use it on the react . only one function is used as default for exporting using the  ES6
// expose these function and variable to other files or modules
// module.exports = {add, substract , age } // used for exposing the multiple  function and varaible to other file or modules

// Core Modules  (built in core modules )
// Contributed Modules
// Thirdty parts Modules
// local or custom modules
// developers list modules for usage

// Node Package Manager (npm )
// it manages different modules  other developer 
// webstie 
// regitsry 
// CLI  to access the module to istall the module inside our project throught the command line 
// Command line inteface to install differ module package inside out project 

