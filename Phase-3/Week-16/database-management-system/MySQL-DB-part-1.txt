Introduction to database and database management system (DBMS)

      - what is DB  (database)
              A database is an organised collection of information or data  typically stored electrocially in a computer system   in the digital form using  computer technology rather than as a physical paper record.

        - How do we control DB
                 - A database is usually controlled by a dbms 
        - what is the dbms 
             - Is a software for easy, efficent and reialbe data processing and management 
             used for 
                -creation of database 
                - store  data /information 
                - retrieval of information from the database
                - controlling redundany and inconsisteney 
                - efficenet memeory management 
                - access control and ease is accessing data
     - Data  + DBMS  + other related apps ===== Database system (Database)  We have covered (MySQL) 


     5.2  Main catergories of database: relational and non-relational Database;

         - relational  Database (RDBMS)
                - store  data in tables  with rows and column 
                - enforce relationships  between   using keys 
                -  structured schema (strict structure) strict data 
                - Uses SQL  FOR queiries (languages)
                - Strong  data integrity
                - slow interms of performace 
                (it requires join of the different table to get the data from two or more three tables ) 
                -  it needs join the table from different tables  to get some data this operation takes a little time  time 
                - strict data 

                     - Example  
                     - MySQL 

         -  Non-relational Database (NoSQL)
               - storing data  in formats like JSON  (key-values ) pairs 
               -  there is  no relation at all between tables 
               - Schema-less or dynamic  schema  (loose format );
               - where data is not strict  the use case    social media 
               - flexible  data bunch of data 
               - easily accessible , fast ,  nested data 
               - performacie data are in documents 
               - non strict  data
               - no table , no relation , it does not need the join all the data are inside the json like object as nested so data accessib is simple and it takes a little time-
               - all the data are  at one place easily accessible 
               - so incase of  non relational database  no table , no  relational 
                   -Example  MongoDB
5.3   Working with databases  using NODE JS:  http verbs  / Methods to communicate with  webserver 
            - GET   -  read data  and recieve it 
            - POST   -  to create  new data  send data to backend  from fronted   forms  then webserver  stores to data  send  data to dtabase base
            -PUT and PATCH 
                -PUT 
                  - update  data 
                  - replaces the entries data  with an new  representation in a single data 
                  - the clients sends  the entries  updated representation of the resouce to the server
                  - rarely used its not common to use 

            - PATCH 
              -  updataed data only  if it exists 
              -  partially updates the data  
              - we use this  https method commonly to updated the specific data  from the databases 
              - the clients sends only specific  changes to be appled to the resoucres  commonly used unlikde th PUT

            - DELETE   
                 - remove data from database through the webserver

                 CRUD OPERATION 
                 C   CREATE    POST 
                 R   READ      GET 
                 U   UPDATED    PUT 
                 D    DELETE   DELETE        HTTPS METHODS 

                 - Matching the crud operation with the https methods or verbs 

                 - GET  -  read  and recieve   fetch the data  from the database 
                 - fetch the data from the database  this is request we have to do 
                 - create   post      new  data  add to the database 
                 - updated  modify the data  
                 - put   replace the whole data   from the database 
                 - patch    change part of the record only  from database 
                 -  delete 


                  // the patters we use 

                  app.get  or post  or put or patch or delete ("/user"     (req, res){
                        // run the quereis sql quires here  
                        //  write the queries programmatically 
                        // select *  from user 

                        //  every routes answer to question 
                        // which https method we use 
                        //  write the   quereis to do the methods 
                        // which https vers we use to  for 
                        // which sql statement   we write 
                        // those two question handerls every api routes  
                        // to create the api routes  ends points of the api 
                        // for comminication of the database with node    node with frontend   two differnet software share data through the api 

                  })

5.4    Working with database using NODe JD :  SQl queries and MySQL  database
- sql quere are the most common  way used by  almost all relation databases to communicate what data  the database provide or storre 
- that means  we need to have a database ready  and we also need away to connect  with the database fromthe  node server  /

-  CREATE 
- INSERT 
- SELECT   WE USE MOST OF THE TIME WE USE THE  SELECTION STATEMENTS 
- UPDATE 
- DELETE     THE MOST COMONLY USED SQL QUERES   THE CRUD OPERATION ON THE DATABASE  THROUGH PROGRAMMING AND MANULLY 

- MySQL   DATABASE (SOFTWARE )
- MySQL   DRIVER 
- CONNECTING  WITH  MYSQL USING NODE 


5.4  Working with databases using NodeJs   : SQL  query and MySQL database;

SQL (structure query language )
    -  is the standard language  for accessing the databases ;
    -   SQL   statements 
         1. CREATE 
         2  INSERT 
         3  SELECT 
         4  UPDATE
         5  DELETE

- MySQL     database
    - A widely use  relational database  management system(RDBMS) 
    -  Free and Open -source 
    -  Ideal for both small and large applications 
5.5  Working with databses using NodeJs: MySQL  driver and connection  with MySQL databases  using NodeJs:

  - MySQL driver
      -  3rd  parts module used to  communicate  between MySQL DB and Node Js ; 
          npm i  mysql      or nmp  i mysql2


      -connect  to the DB from NodeJs
         - on mysql  Db unders phymyadim  
          --create the  databases 
          -- add username and password for the created databases and give the privilite for the user on that  databases
      - ON  NodeJS 
         - istall and import   the  moduele that the appliction needs   the mysql2  
         - use createConnection method and add the credential infomration in object form 
         -connect to the DB using the connect method 
















